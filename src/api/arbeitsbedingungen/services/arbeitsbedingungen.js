'use strict';

/**
 * arbeitsbedingungen service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::arbeitsbedingungen.arbeitsbedingungen');
